import java.util.ArrayList;

/**
 * TimeSeriesPredictor - takes input of keywords and time series from the user
 * and provides the predicted values on the basis of six different prediction models
 * 
 * @author Jehan Sethna
 * @version 1.0 Built on 4/6/2012
 */

public class TimeSeriesPredictor
{
	/**
	 * The predictor() receives the different input values from the GUI which will be entered by the 
	 * user, calculates the appropriate values to be predicted and returns an object containing 
	 * the results back to the GUI
	 * @param timeseries_and_date - ArrayList of Objects consisting the time_series_value and 
	 * 								corresponding date for that input value
	 * @param no_of_future_values - Integer value which shows the number of values to be predicted
	 * @param keywords - String Array consisting of the keywords 
	 * @return - an object will be returned back to the GUI. The object will contain the prediction 
	 * model along with the predicted value for the number of values to be predicted
	 */
	public ArrayList<Object> predictor(ArrayList<Object> timeseries_and_date, int no_of_future_values, String[] keywords)
		{
			return obj;
		}
	

}
